# Pins the public npm registry for this project regardless of the local
# or CI machine's global/user npm config — this repo has no dependency
# on any private registry, and a machine logged into a private one
# (e.g. a company registry) should never affect how Vercel or anyone
# else installs these packages.
registry=https://registry.npmjs.org/
